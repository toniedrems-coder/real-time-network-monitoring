as a backend developer for years trying to break into the world of front end engineering 
i have finally understood the basic flow of a react app start up and serve its initial page

when the index.html is requested by a client 
the page executes the script tag which has main,jsx

main.jsx executes under the strict mode the app component 
the appcomponebt executes the AppRoute component

the approutes executes the routes that matches the requested 
or serve the default route with the page configured

the page served executes the pageView component 
where all the gynastics are downtime

the resultlresponse is placed inside the the div with id as root in the index.htl/default page

Praise God
